# Use a flat node_modules layout. Required for the Dockerfile runtime stage
# which COPY's individual native-module dirs (better-sqlite3, bindings,
# file-uri-to-path) from the build stage. With the default pnpm layout those
# packages live under .pnpm/ and the COPY paths don't exist.
node-linker=hoisted
